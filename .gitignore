node_modules/
dist/
*.log

# Phase 5 staleness spike — local-only working data (real content from a
# separate private repository; never committed) and disposable scratch
# copies the threshold sweep generates.
spike/*/.data/
src/provenance/*.sweep-scratch.ts
test-results/
playwright-report/

# Cards/findings generated by running illuminate on its own demo artifact.
.demo/*.illum.json
.demo/*.illum-findings.json
.demo/self-export.html

# Ad-hoc browser-driving scripts used while diagnosing the chrome.
.scratch/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Internal build scaffolding. These exist in the private working tree and are
# never published: planning docs, the agent config used to build this tool, and
# the pre-publication checklist — all of which reference private work.
# The rest of .demo/ IS published: e2e/demo-artifact.spec.ts runs against it.
.planning/
.claude/
.lavish/
spike/
.mcp.json
.demo/VALIDATION.md
